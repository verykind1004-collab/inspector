<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
  Summary Check 진단 SQL. databaseId 로 Oracle/PG 분기(원본의 db_type 분기와 동등).
  원본: Labs 변형(release/inspector/Labs/Inspector/python-utils/sql_library.py)
        _SQL_SUMMARY_10MIN / _SQL_SUMMARY_1HOUR / _SQL_PG_SUMMARY_10MIN / _SQL_PG_SUMMARY_1HOUR

  1:1 동등 포팅 원칙:
   - SQL 본문(WHERE/JOIN/CASE/컬럼 alias/세미콜론 외 모든 토큰)을 원본 그대로 유지.
   - 제거 가능한 것은 JDBC 무관 부분뿐 = SQL*Plus 지시어(SET/COLUMN), psql 메타.
   - ResultSet 라벨 -> SummaryRow 필드는 resultMap 으로 명시(원본 alias 보존).
-->
<mapper namespace="com.exem.inspector.screen.summary.SummaryMapper">

    <!-- 원본 alias 보존: "DB ID"/INSTANCE_NAME/SUMMARY_TYPE/LAST_SUMMARY/STATUS/DELAY_INFO -->
    <resultMap id="summaryRow" type="com.exem.inspector.screen.summary.SummaryRow">
        <result column="DB ID"        property="dbId"/>
        <result column="INSTANCE_NAME" property="instanceName"/>
        <result column="SUMMARY_TYPE"  property="summaryType"/>
        <result column="LAST_SUMMARY"  property="lastSummary"/>
        <result column="STATUS"        property="status"/>
        <result column="DELAY_INFO"    property="delayInfo"/>
    </resultMap>

    <!-- ── 10Min Summary (Oracle) — _SQL_SUMMARY_10MIN ──────────────────── -->
    <select id="findSummary10Min" resultMap="summaryRow" databaseId="oracle">
        WITH expect AS (
            SELECT TRUNC(SYSDATE,'HH24')
                 + ((FLOOR(TO_NUMBER(TO_CHAR(SYSDATE,'MI'))/10)*10)-10)/1440 AS expected_time,
                   TRUNC(SYSDATE) AS today FROM dual),
        sum_raw AS (
            SELECT a.db_id, a.instance_name, b.summary_type, b.summary_time, e.expected_time,
                   CASE WHEN b.summary_time IS NULL THEN 'CHECK'
                        WHEN b.summary_time=e.expected_time THEN 'OK'
                        WHEN TRUNC(b.summary_time)=e.today THEN 'WAITING'
                        ELSE 'CHECK' END AS status
            FROM apm_db_info a
            LEFT JOIN ora_last_summary b ON a.db_id=b.server_id AND b.summary_type LIKE '%10Min%'
            CROSS JOIN expect e)
        SELECT
            db_id AS "DB ID",
            instance_name AS INSTANCE_NAME,
            summary_type  AS SUMMARY_TYPE,
            TO_CHAR(summary_time, 'YYYY-MM-DD HH24:MI:SS') AS LAST_SUMMARY,
            status AS STATUS,
            CASE
                WHEN status = 'OK' THEN NULL
                WHEN status = 'WAITING' THEN
                    '+' || FLOOR(ROUND((expected_time - summary_time) * 1440) / 60) || 'h '
                        || MOD(ROUND((expected_time - summary_time) * 1440), 60) || 'm'
                WHEN status = 'CHECK' AND summary_time IS NOT NULL THEN
                    '+' || CEIL(expected_time - summary_time) || 'd'
                ELSE NULL
            END AS DELAY_INFO
        FROM sum_raw
        ORDER BY
            summary_time ASC NULLS FIRST,
            db_id ASC
    </select>

    <!-- ── 10Min Summary (PostgreSQL) — _SQL_PG_SUMMARY_10MIN ───────────── -->
    <select id="findSummary10Min" resultMap="summaryRow" databaseId="postgresql">
        WITH expect AS (
            SELECT date_trunc('hour', localtimestamp)
                   + (((extract(minute from localtimestamp)::int / 10) * 10) - 10) * interval '1 minute' AS expected_time,
                   current_date AS today
        ),
        sum_raw AS (
            SELECT a.db_id, a.instance_name, b.summary_type, b.summary_time, e.expected_time,
                   CASE WHEN b.summary_time IS NULL THEN 'CHECK'
                        WHEN date_trunc('minute', b.summary_time) = e.expected_time THEN 'OK'
                        WHEN date_trunc('day', b.summary_time) = e.today THEN 'WAITING'
                        ELSE 'CHECK' END AS status
            FROM apm_db_info a
            LEFT JOIN ora_last_summary b ON a.db_id = b.server_id AND b.summary_type LIKE '%10Min%'
            CROSS JOIN expect e
        )
        SELECT
            db_id AS "DB ID",
            instance_name AS INSTANCE_NAME,
            summary_type AS SUMMARY_TYPE,
            to_char(summary_time, 'YYYY-MM-DD HH24:MI:SS') AS LAST_SUMMARY,
            status AS STATUS,
            CASE
                WHEN status = 'OK' THEN NULL
                WHEN status = 'WAITING' THEN
                    '+' || (round(extract(epoch from (expected_time - summary_time)) / 60)::int / 60) || 'h '
                        || (round(extract(epoch from (expected_time - summary_time)) / 60)::int % 60) || 'm'
                WHEN status = 'CHECK' AND summary_time IS NOT NULL THEN
                    '+' || ceil(extract(epoch from (date_trunc('day', expected_time) - date_trunc('day', summary_time))) / 86400.0)::int || 'd'
                ELSE NULL
            END AS DELAY_INFO
          FROM sum_raw
         ORDER BY
          summary_time ASC NULLS FIRST,
          db_id ASC
    </select>

    <!-- ── 1Hour (Daily) Summary (Oracle) — _SQL_SUMMARY_1HOUR ──────────── -->
    <select id="findSummary1Hour" resultMap="summaryRow" databaseId="oracle">
        WITH expect AS (
            SELECT TRUNC(SYSDATE - INTERVAL '1' HOUR, 'HH24') AS expected_time,
                   TRUNC(SYSDATE) AS today FROM dual),
        sum_raw AS (
            SELECT a.db_id, a.instance_name, b.summary_type, b.summary_time, e.expected_time,
                   CASE WHEN b.summary_time IS NULL THEN 'CHECK'
                        WHEN b.summary_time = e.expected_time THEN 'OK'
                        WHEN TRUNC(b.summary_time) = e.today THEN 'WAITING'
                        ELSE 'CHECK' END AS status
            FROM apm_db_info a
            LEFT JOIN ora_last_summary b ON a.db_id = b.server_id AND b.summary_type LIKE '%Daily%'
            CROSS JOIN expect e)
        SELECT
            db_id AS "DB ID",
            instance_name AS INSTANCE_NAME,
            summary_type  AS SUMMARY_TYPE,
            TO_CHAR(summary_time, 'YYYY-MM-DD HH24:MI:SS') AS LAST_SUMMARY,
            status AS STATUS,
            CASE
                WHEN status = 'OK' THEN NULL
                WHEN status = 'WAITING' THEN '+' || ROUND((expected_time - summary_time) * 24) || 'h'
                WHEN status = 'CHECK' AND summary_time IS NOT NULL THEN '+' || CEIL(expected_time - summary_time) || 'd'
                ELSE NULL
            END AS DELAY_INFO
        FROM sum_raw
        ORDER BY
            summary_time ASC NULLS FIRST,
            db_id ASC
    </select>

    <!-- ── 1Hour (Daily) Summary (PostgreSQL) — _SQL_PG_SUMMARY_1HOUR ───── -->
    <select id="findSummary1Hour" resultMap="summaryRow" databaseId="postgresql">
        WITH expect AS (
            SELECT date_trunc('hour', localtimestamp - interval '1 hour') AS expected_time,
                   date_trunc('day', localtimestamp) AS today
        ),
        sum_raw AS (
            SELECT a.db_id, a.instance_name, b.summary_type, b.summary_time, e.expected_time,
                   CASE WHEN b.summary_time IS NULL THEN 'CHECK'
                        WHEN date_trunc('minute', b.summary_time) = date_trunc('minute', e.expected_time) THEN 'OK'
                        WHEN date_trunc('day', b.summary_time) = e.today THEN 'WAITING'
                        ELSE 'CHECK' END AS status
            FROM apm_db_info a
            LEFT JOIN ora_last_summary b ON a.db_id = b.server_id AND b.summary_type LIKE '%Daily%'
            CROSS JOIN expect e
        )
        SELECT
            db_id AS "DB ID",
            instance_name AS INSTANCE_NAME,
            summary_type AS SUMMARY_TYPE,
            to_char(summary_time, 'YYYY-MM-DD HH24:MI:SS') AS LAST_SUMMARY,
            status AS STATUS,
            CASE
                WHEN status = 'OK' THEN NULL
                WHEN status = 'WAITING' THEN
                    '+' || round(extract(epoch from (expected_time - summary_time)) / 3600)::int || 'h'
                WHEN status = 'CHECK' AND summary_time IS NOT NULL THEN
                    '+' || ceil(extract(epoch from (date_trunc('day', expected_time) - date_trunc('day', summary_time))) / 86400.0)::int || 'd'
                ELSE NULL
            END AS DELAY_INFO
          FROM sum_raw
         ORDER BY
          summary_time ASC NULLS FIRST,
          db_id ASC
    </select>

</mapper>
